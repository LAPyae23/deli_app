# Notification sounds

Add a short notification sound file here named:

`notification.mp3`

It will be served at `/assets/sounds/notification.mp3` and used by restaurant order alerts, rider dispatch alerts, and chat message notifications.
